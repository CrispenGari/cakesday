mutation ChangePassword($input: ChangePasswordInputType!) {
  changePassword(input: $input) {
    user {
      id
      username
      isLoggedIn
    }
    accessToken
    error {
      field
      message
    }
  }
}
